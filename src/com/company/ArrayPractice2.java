package com.company;
import java.util.Arrays;

public class ArrayPractice2 {
    public static void main(String[] args){
        //  Array numbers = new Array(length:3);
        //  numbers.insert(item:10);
        //  numbers.insert(item:20);
        //  numbers.insert(item:30);
        //  numbers.insert(item:40);
        //  numbers.removeAt(index:3);     will remove 40 from index
        //  numbers.print();
        // System.out.println(numbers.indexOf(10));  will print 0 since that is the index of 10, will return -1 if number isn't in array


    }
}
